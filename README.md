# Parlabuhan

Parlabuhan — simple local container browser for developers.

## Local dev

```bash
# run locally (needs docker client/socket access)
go run main.go
# open http://localhost:80  (or set PORT env)
```
